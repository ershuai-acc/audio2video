# 音频转视频生成器

将音频文件转换为带字幕的视频，支持 AI 生成封面或自定义图片。

## 功能特性

- 📁 **上传音频** - 支持 MP3、WAV、M4A 格式
- 📝 **字幕选项**
  - ASR 自动语音识别
  - 手动输入字幕文本（零 API 成本）
  - 无字幕模式
- 🖼️ **底图选项**
  - AI 生成底图
  - 上传自定义图片 + 裁剪
- 📐 **视频比例** - 9:16 竖屏 / 16:9 横屏
- ✂️ **图片裁剪** - Cropper.js 支持缩放、移动选择局部
- 🎨 **字幕样式** - 字体大小、颜色、描边设置
- 📋 **历史记录** - 本地存储，方便查看和下载

## 快速开始

```bash
# 安装依赖
npm install

# 启动服务
node server.mjs

# 访问
open http://localhost:3000
```

## 项目结构

```
audio-to-video-app/
├── public/
│   └── index.html      # 前端页面
├── server.mjs          # Express 后端服务
├── package.json        # 依赖配置
├── .gitignore          # Git 忽略配置
└── README.md           # 项目说明
```

## Git 排除说明

以下目录/文件不包含在 Git 仓库中：

| 目录/文件 | 说明 |
|-----------|------|
| `node_modules/` | npm 依赖包，运行 `npm install` 自动生成 |
| `.piebox/` | API 凭据（敏感信息），需自行配置 |
| `uploads/` | 用户上传的音频文件，运行时自动创建 |
| `outputs/` | 生成的图片和视频文件，运行时自动创建 |

## 配置凭据

首次运行前，需要创建 `.piebox/env` 文件：

```bash
mkdir -p .piebox
cat > .piebox/env << EOF
PIE_APP_ID=your_app_id
PIE_APP_SECRET=your_app_secret
PIE_GATEWAY_PATH=https://pie-gateway.weapp.me
EOF
```

## 依赖

- **后端**: Express, Multer, CORS
- **前端**: Cropper.js (CDN)
- **工具**: FFmpeg (需系统安装)

## License

MIT
